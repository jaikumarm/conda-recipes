package:
  name: keras
  version: "1.0.2"

source:
  fn: Keras-1.0.2.tar.gz
  url: https://pypi.python.org/packages/3e/a3/eca460432e38931e55b7454f52c5740943b9df9bb18e103ca37cad32d463/Keras-1.0.2.tar.gz
  md5: efe73460e0d12ab6d6deb953dc58bd98
#  patches:
   # List any patch files here
   # - fix.patch

# build:
  # noarch_python: True
  # preserve_egg_dir: True
  # entry_points:
    # Put any entry points (scripts to be generated automatically) here. The
    # syntax is module:function.  For example
    #
    # - keras = keras:main
    #
    # Would create an entry point called keras that calls keras.main()


  # If this is a new build for the same version, increment the build
  # number. If you do not include this key, it defaults to 0.
  # number: 1

requirements:
  build:
    - python
    - setuptools
    - theano
    - pyyaml
    - six

  run:
    - python
    - theano
    - pyyaml
    - six

test:
  # Python imports
  imports:
    - keras
    - keras.backend
    - keras.datasets
    - keras.engine
    - keras.layers
    - keras.legacy
    - keras.preprocessing
    - keras.utils
    - keras.wrappers

  # commands:
    # You can put test commands to be run here.  Use this to test that the
    # entry points work.


  # You can also put a file called run_test.py in the recipe that will be run
  # at test time.

  # requires:
    # Put any additional test requirements here.  For example
    # - nose

about:
  home: https://github.com/fchollet/keras
  license: MIT
  summary: 'Deep Learning for Python'

# See
# http://docs.continuum.io/conda/build.html for
# more information about meta.yaml
